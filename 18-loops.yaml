- name: installing multiple packages 
  hosts: local
  connection: local
  become: yes 
  tasks:
  - name: multiple packages 
    ansible.builtin.package:
      name: "{{ item }}"
      state: installed 
  loop:
    - gcc
    - git
    - python 
